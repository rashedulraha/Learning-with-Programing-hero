function multiply(x, y) {
  let xa = x + 5;
  let ya = y + 5;
  const multiply = xa + ya;
  return multiply;
}

// const result = multiply(2, 5);
// console.log(result);

const manik = {
  name: "manik",
  balance: 20000,
};
const raton = {
  name: "raton",
  balance: 80080,
};

console.log("before Call", manik, raton);

function totalMoney(person1, person2) {}
