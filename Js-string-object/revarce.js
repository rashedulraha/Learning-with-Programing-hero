const sentence = "I'm learning web dev";

let revers = "";
for (latter of sentence) {
  // console.log(latter);
  revers = latter + revers;
}

for (let rever = 0; rever > sentence.length; rever++) {
  // revers =
  console.log(rever);
}

console.log(revers);
